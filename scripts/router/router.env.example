# Moved: the single documented example for ALL variables is ../../holocortex.env.example
# (v0.6 config externalisation, ADR-0006). Copy that file to router.env here for
# the daemon container; copy it to ~/.config/holocortex/env for the CLI clients.
